## Artifacts

This directory stores local/generated/imported artifacts that are not part of the app source.

- `tmp/` - temporary JSON files.
- `imports/` - one-off source imports (Ghost export, legacy markdown dumps).
- `graphify/` - graphify output.
- `tooling/` - local tool state (for example, Wrangler cache/state).

These files are ignored by git.
